
```mermaid 
graph TD    

DUT[reg_module DUT]    

VIF[reg_if 接口]    AGT[agent]    DRV[driver]    MON[monitor]    SQR[sequencer]    SEQ[sequence]    SCB[scoreboard]    DUT <-->|引脚信号| VIF    VIF <--> AGT    AGT --> DRV    AGT --> MON    AGT --> SQR    SQR --> SEQ    SEQ -->|生成 item| SQR    SQR -->|传 item| DRV    DRV -->|write/read| VIF    MON -->|采样| VIF    MON -->|write| SCB```
````
